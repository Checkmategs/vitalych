{# Пояснительная записка к эскизному / техническому проекту — ГОСТ Р 59795—2021 п. 5.2 #}
{# Единый контекст: тот же project.yaml, что и для ТЗ #}
{% macro fill(value, hint) -%}
{% if value is defined and value is string and value|trim -%}
{{ value }}
{%- else -%}
_заполнить: {{ hint }}_
{%- endif %}
{%- endmacro %}

# Пояснительная записка к эскизному (техническому) проекту

**Автоматизированная система:** {{ system.name }} ({{ system.short_name }})

---

## 1. Общие положения

### 1.1. Наименование АС и основания проектирования

- **Проектируемая АС:** {{ system.name }} ({{ system.short_name }})
- **Шифр темы:** {{ system.topic_code | default('—') }}

{% if basis.documents %}
Документы, на основании которых ведут проектирование:

| Документ | Номер | Дата | Утверждён |
|----------|-------|------|-----------|
{% for d in basis.documents %}
| {{ d.name }} | {{ d.number }} | {{ d.date }} | {{ d.approved_by | default('—') }} |
{% endfor %}
{% else %}
_заполнить: наименования документов, номера и даты утверждения_
{% endif %}

### 1.2. Организации и сроки стадий

- **Заказчик:** {{ parties.customer }}
- **Разработчик:** {{ parties.developer }}
{% if parties.participants %}
- **Участники:**
{% for p in parties.participants %}
  - {{ p.name }} — {{ p.role }}
{% endfor %}
{% endif %}

- **Общий срок работ:** {{ schedule.start }} — {{ schedule.end }}
{% if schedule.stages %}
- **Стадии:**
{% for s in schedule.stages %}
  - {{ s.name }}: {{ s.start }} — {{ s.end }}
{% endfor %}
{% endif %}

### 1.3. Цели, назначение и области использования

- **Назначение:** {{ fill(goals.purpose, 'назначение и области использования АС') }}
{% if goals.objectives %}
- **Цели:**
{% for o in goals.objectives %}
  - {{ o.name }} ({{ o.target_value }}; критерий: {{ o.criterion }})
{% endfor %}
{% endif %}

### 1.4. Соответствие нормам безопасности

{{ fill(pz.safety_compliance, 'подтверждение соответствия нормам ТБ, пожаро- и взрывобезопасности и т. п.') }}

### 1.5. Нормативно-технические документы

{{ fill(pz.normative_docs, 'сведения об использованных при проектировании НТД') }}

### 1.6. НИР, передовой опыт, изобретения

{{ fill(pz.research_and_inventions, 'сведения о НИР, опыте, изобретениях (при наличии)') }}

### 1.7. Очередность создания АС

{{ fill(pz.queues, 'очередность создания АС и объём каждой очереди') }}

---

## 2. Описание процессов деятельности объекта автоматизации

### 2.1. Объект автоматизации

{{ fill(object.description, 'основные сведения об объекте') }}

### 2.2. Состав процедур (операций)

{{ fill(object.processes, 'состав процедур с учётом взаимосвязи автоматизированной и неавтоматизированной деятельности') }}

### 2.3. Организация работ в условиях функционирования АС

{{ fill(pz.work_organization, 'требования к организации работ при функционировании АС') }}

---

## 3. Основные технические решения

### 3.1. Структура АС и взаимодействие компонентов

{{ fill(requirements.structure, 'структура АС/подсистем, взаимодействие, связи компонентов; смежные системы и совместимость; режимы и диагностирование') }}

### 3.2. Персонал АС

{{ fill(requirements.general.personnel, 'численность, квалификация, функции персонала, режимы работы') }}
{{ fill(requirements.support.organizational, 'организационное обеспечение') }}

### 3.3. Обеспечение потребительских характеристик (качество относительно ТЗ)

{{ fill(pz.quality_vs_tz, 'сведения об обеспечении заданных в ТЗ характеристик качества') }}

### 3.4. Состав функций, комплексов задач

{% if requirements.functions %}
{% for f in requirements.functions %}
- **{{ f.name }}** — {{ f.result }}
{% endfor %}
{% else %}
_заполнить: состав функций, комплексов задач (задач)_
{% endif %}

### 3.5. Комплекс технических средств

{{ fill(requirements.support.technical, 'решения по КТС и размещению на объекте') }}

### 3.6. Информационное обеспечение

{{ fill(requirements.support.information, 'состав информации, организация, входные/выходные документы и сообщения') }}

### 3.7. Программное обеспечение

{{ fill(requirements.support.software, 'состав ПО, языки, алгоритмы процедур/операций и методы реализации') }}

### 3.8. Иллюстрации смежных документов проекта

{{ fill(pz.illustrations_note, 'при необходимости — иллюстрации других документов по ГОСТ 34.201') }}

---

## 4. Мероприятия по подготовке объекта автоматизации к вводу АС в действие

### 4.1. Приведение информации к виду для СВТ

{{ fill(prep.information, 'мероприятия по подготовке данных') }}

### 4.2. Обучение и проверка квалификации персонала

{{ fill(prep.training, 'обучение персонала') }}

### 4.3. Создание подразделений и рабочих мест

{{ fill(prep.org_staff, 'оргштатные мероприятия') }}

### 4.4. Изменение объекта автоматизации

{{ fill(prep.object_changes, 'мероприятия по изменению объекта') }}

### 4.5. Иные мероприятия

{{ fill(prep.other, 'по специфике создаваемой АС') }}

---

*Документ сформирован по структуре ГОСТ Р 59795—2021 п. 5.2 (пояснительные записки к эскизному, техническому проектам).*
